schema {
    query: Query
    mutation: Mutation
}

type Query {
    Mixer: Mixer!

    Modules: ModulesQuery!
}

type Mutation {
    Mixer: MixerMutation!

    Module(Idx: Int!): ModuleMutation!
    Modules: ModulesMutation!
}